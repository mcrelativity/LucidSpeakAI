# FastAPI and dependencies
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.19
slowapi==0.1.9

# Database
supabase==2.10.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Audio processing
librosa==0.10.2
soundfile==0.12.1
numpy==2.1.3
scipy==1.14.1
pydub==0.25.1
audioread==3.0.1

# Payment Processing
stripe>=10.0.0

# Environment management
python-dotenv==1.0.1

# ============================================================================
# PRO AUDIO ANALYSIS FEATURES (Pro/Pro+ Users)
# ============================================================================

# OpenAI Integration
openai>=1.100.0

# Prosody Analysis (Pitch, Intensity, Speaking Rate) - OPTIONAL
# Requires: Praat installed on system (see docs/PRO_AUDIO_ANALYSIS_ARCHITECTURE.md)
# parselmouth==0.5  # Uncomment if needed for prosody features

# Advanced Audio Features (Optional: for enhanced analysis)
# essentia==2.1b6.dev20240523  # Uncomment for music/audio feature extraction

# Emotion Detection & Speaker Diarization (Optional for Pro+ tier)
# Note: pyannote-audio requires PyTorch, may increase deployment size
# pyannote-audio==3.1.1  # Uncomment for emotion detection

# Deep Learning (Required for emotion models if using pyannote)
# torch==2.1.0  # Uncomment if enabling emotion detection
# transformers==4.45.0  # Uncomment if enabling emotion detection

# ============================================================================
# System dependencies note:
# ffmpeg is required for pydub (installed via apt-get on Render)
# Praat is required for parselmouth (optional for pro features)
# ============================================================================
